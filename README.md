# How my program works

i dont understand it myself, but let me try to explain it.

## the algorithm
if we have a `p` that is a prime number, and if `(2^p) -1` is also a prime, then `(2^p-1)*((2^p)-1)` is a perfect number!
i wrote 2 functions (`exponent()` and `isPrime()`). its pretty obvious what do they do.

the algorithm is basically this: 
- increase `p` until its prime
- if `p` is a `mersene prime` print it
- if not repeat until it is
- do this until we have 8 perfect numbers

## how the code works
not gonna lie, i dont know how it does.

as you see, almost everything is `unsinged long long` because `int` is not enough.
i am too low skilled to fix this.

## another important detail
my functions are pretty shitty. dont trust them your life savings.

# bye!









